// No server-side acts yet: in mock the fixture already contains the player's
// Deployment (so the check passes as if they deployed it); live auto-play acts
// land with the NKP deploy work. Kept as empty maps so the loader is happy.
export const acts = {};
export const cleanups = {};
