{
  "name": "ntnx-infiltration-game",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "bun --env-file=.env --watch packages/server/src/index.ts",
    "dev:frontend": "cd packages/frontend && bun run dev",
    "build": "bun run build:frontend && bun run build:server",
    "build:frontend": "cd packages/frontend && bun run build",
    "build:server": "bun build packages/server/src/index.ts --target=bun --outdir=dist/server",
    "test": "bun test",
    "typecheck": "cd packages/engine && bun run typecheck && cd ../server && bun run typecheck && cd ../nutanix && bun run typecheck && cd ../frontend && bun run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/bun": "^1.3.0"
  }
}
